<h1>Listing gifts</h1>

<table>
  <tr>
    <th>Confirmation code</th>
    <th>Zip</th>
    <th>Gift code</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @gifts.each do |gift| %>
  <tr>
    <td><%= gift.confirmation_code %></td>
    <td><%= gift.zip %></td>
    <td><%= gift.gift_code %></td>
    <td><%= link_to 'Show', gift %></td>
    <td><%= link_to 'Edit', edit_gift_path(gift) %></td>
    <td><%= link_to 'Destroy', gift, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Gift', new_gift_path %>
