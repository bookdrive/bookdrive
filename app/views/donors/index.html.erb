<h1>Listing donors</h1>


<%= form_tag donors_path, :method => 'get', :id => "donors_search", :class => "search_form" do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
  </p>
  <div id="donors"><%= render 'donors' %></div>
<% end %>

